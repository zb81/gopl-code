package main

//func main() {
//
//	//s := "Hello, world!"
//
//	// len 返回一个字符串中的字节数目，第 i 个字节并不一定是字符串的第 i 个字符
//	//fmt.Println(len(s))
//	//fmt.Println(s[0], s[7])
//
//	//c := s[len(s)]
//	//fmt.Println(c)
//
//	// 子字符串操作 s[i:j] 基于原始的 s 字符串的第 i 个子节开始，到第 j 个子节（不包含 j 本身），生成一个新字符串
//	// 生成的新字符串将包含 j-i 个子节
//	//fmt.Println(s[0:5]) // Hello
//
//	// + 操作符将两个字符串连接，构造一个新字符串：
//	//fmt.Println("goodbye" + s[5:]) // goodbye, world!
//
//	// 字符串可以用 == 和 < 进行比较；通过逐个子节比较完成（字符串自然编码的顺序）
//
//	// 字符串的值是不可变的：一个字符串包含的字节序列永远不会被改变
//	//s := "left foot"
//	//t := s
//	//s += ", right foot"
//	//fmt.Println(s)
//	//fmt.Println(t)
//
//	// 因为字符串是不可修改的，因此尝试修改字符串内部数据也是被禁止的
//	//s[0] = 'L'
//
//}
